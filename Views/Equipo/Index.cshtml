@model IEnumerable<MvcEquipo.Models.Equipo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.deporte)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.patrocinador)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.deporte)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.patrocinador.apellido)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.equipoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.equipoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.equipoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
